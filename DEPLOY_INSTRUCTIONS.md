# 🚀 تعليمات النشر (Deployment Instructions)

## 1. رفع الملفات (Upload)
يجب رفع محتويات مجلد `out` **بالكامل**.

### الحالة A (الموقع على `https://yourdomain.com/archive/`)
- ارفع **محتويات** `out` (وليس المجلد نفسه) إلى: `public_html/archive/`
- تأكد بعد الرفع أن المسارات التالية موجودة على السيرفر:
	- `/archive/index.html`
	- `/archive/_next/` (مجلد كامل)
	- `/archive/dashboard/`

مهم: لا ترفع `out/archive` وحده، لأن هذا سيُفقدك مجلد `_next` وبالتالي التحديثات لن تظهر.

**الملفات المهمة جداً:**
- `index.html` (يحتوي على الإصلاح النهائي)
- `clear-cache.html` (لإصلاح مشاكل الدخول)
- `wp-includes/` (لمنع أخطاء 404)
- `_next/` (ملفات النظام)

## 2. مسح الكاش (Clear Cache) - خطوة إجبارية!
بعد الرفع، يجب عليك وعلى جميع المستخدمين تنفيذ هذه الخطوة مرة واحدة:

1. افتح الرابط: `https://zaco.sa/archive/clear-cache.html`
2. اضغط على زر **"مسح جميع البيانات"**.
3. انتظر حتى يتم توجيهك لصفحة الدخول.
4. سجل دخولك.

## 3. لماذا هذه الخطوات؟
- **مشكلة الشاشة البيضاء (Illegal constructor):** تم حلها في `index.html` الجديد. إذا لم ترفع الملف الجديد، ستظل المشكلة.
- **مشكلة Access token required:** سببها بيانات قديمة في المتصفح. صفحة `clear-cache.html` تحذفها بالكامل.
- **مشكلة wp-emoji:** تم حلها برفع مجلد `wp-includes`.

## 4. التحقق
إذا ظهرت أي مشكلة بعد ذلك، اضغط `F12` وصور الشاشة (Console) وأرسلها لنا.
لكن مع هذا التحديث، يجب أن يعمل كل شيء بنسبة 100%.
