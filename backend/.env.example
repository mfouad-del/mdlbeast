# Backend example env file
# Replace placeholder values with your real values in a non-committed .env file
DATABASE_URL=postgresql://zacodb_user:<PASSWORD>@dpg-host.example.com/zacodb
JWT_SECRET=your_jwt_secret_here
PORT=3001
NODE_ENV=production
FRONTEND_URL=https://zaco.sa
